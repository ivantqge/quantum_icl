# Core dependencies for the LLM circuit-synthesis pipeline.
# Setup on NERSC Perlmutter:
#   module load python/3.11-24.1.0
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
cirq-core>=1.3
numpy>=1.24
openai>=1.30
matplotlib>=3.7
pyyaml>=6.0

# Optional LLM backends (not needed for the grok/mock backends):
# anthropic>=0.40
# google-generativeai>=0.8

# Testing:
pytest>=7.4
